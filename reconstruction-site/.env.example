# Copy to .env.local in reconstruction-site. Keys are server-side only.
RECONSTRUCTION_PROVIDER=fal
FAL_KEY=

# Server-only Python converter endpoint and bearer credential (HTTPS required).
# Never use NEXT_PUBLIC_ for these values.
CONVERTER_URL=
CONVERTER_TOKEN=
